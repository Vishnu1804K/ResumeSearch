server:
  port: 8080

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/resumesearch}

  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

# CORS and WebSocket configuration will be refined as controllers and handlers are added.

